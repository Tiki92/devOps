version: 0.2
phases:
  install:
    runtime-versions:
      docker: 18
  build:
    commands:
        - ls
        - docker -v
        - docker-compose -v
        - docker start
        - docker-compose build
        - docker-compose -f docker-compose.yml up
        
