version: 0.2
phases:
  install:
    runtime-versions:
      docker: 18
  build:
    commands:
        - ls
        - sudo docker info
        - docker-compose --version
        - docker-compose down
        - docker-compose build
        - docker-compose up
